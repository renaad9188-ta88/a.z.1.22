# دليل الوصول إلى لوحة الإدارة

## 🔗 رابط لوحة الإدارة

**الرابط المباشر:** `http://localhost:3000/admin`

أو بعد تسجيل الدخول، سيتم توجيهك تلقائياً إلى `/admin` إذا كنت إدارياً.

---

## 📝 خطوات تسجيل الدخول كإداري

### 1. اذهب إلى صفحة تسجيل الدخول
- الرابط: `http://localhost:3000/auth/login`

### 2. اختر "إيميل (إداري)"
- اضغط على زر "إيميل (إداري)" في أعلى النموذج

### 3. أدخل بيانات الدخول
- **الإيميل:** `tamer88@gmail.com`
- **كلمة المرور:** `123456123`

### 4. اضغط "تسجيل الدخول"
- سيتم توجيهك تلقائياً إلى `/admin`

---

## ⚠️ حل مشكلة خطأ 500

إذا ظهر خطأ 500 عند محاولة فتح لوحة الإدارة، نفّذ الخطوات التالية:

### الخطوة 1: نفّذ ملف SQL الإصلاحي

1. اذهب إلى **Supabase Dashboard** > **SQL Editor**
2. افتح ملف `supabase/FIX_RLS_SIMPLE_FINAL.sql`
3. انسخ كل المحتوى والصقه في SQL Editor
4. اضغط **Run**

هذا الملف سيقوم بـ:
- ✅ حذف التكرارات من `profiles`
- ✅ إضافة UNIQUE constraint
- ✅ حذف جميع السياسات القديمة
- ✅ إنشاء سياسات جديدة صحيحة
- ✅ إنشاء profile للمستخدم `tamer88@gmail.com` وتعيينه كإداري

### الخطوة 2: امسح Cache المتصفح

1. اضغط `Ctrl + Shift + Delete`
2. اختر "Cached images and files"
3. اضغط "Clear data"

أو استخدم **Incognito Mode**:
- اضغط `Ctrl + Shift + N`
- سجّل الدخول مرة أخرى

### الخطوة 3: سجّل الدخول مرة أخرى

1. اذهب إلى `/auth/login`
2. اختر "إيميل (إداري)"
3. أدخل `tamer88@gmail.com` و `123456123`
4. اضغط "تسجيل الدخول"

### الخطوة 4: اذهب إلى `/admin`

- يجب أن تعمل لوحة الإدارة الآن بدون أخطاء

---

## 🔍 التحقق من أن كل شيء يعمل

بعد تنفيذ ملف SQL، يمكنك التحقق من:

1. **التحقق من المستخدمين الإداريين:**
   ```sql
   SELECT p.user_id, p.full_name, p.role, u.email
   FROM profiles p
   LEFT JOIN auth.users u ON u.id = p.user_id
   WHERE p.role = 'admin';
   ```

2. **التحقق من السياسات:**
   ```sql
   SELECT tablename, policyname
   FROM pg_policies 
   WHERE tablename IN ('profiles', 'visit_requests')
   ORDER BY tablename, policyname;
   ```

---

## 📞 إذا استمرت المشكلة

1. تأكد من تنفيذ ملف `FIX_RLS_SIMPLE_FINAL.sql` بالكامل
2. تأكد من أن المستخدم `tamer88@gmail.com` موجود في `auth.users`
3. تأكد من أن profile موجود في `profiles` مع `role = 'admin'`
4. امسح Cache المتصفح تماماً
5. أعد تشغيل الخادم (`npm run dev`)

---

## 🎯 الميزات المتوفرة في لوحة الإدارة

- ✅ عرض جميع طلبات المستخدمين
- ✅ البحث والتصفية
- ✅ عرض تفاصيل كل طلب
- ✅ عرض صور الجوازات والدفعات
- ✅ تحديث حالة الطلب
- ✅ الرد على المستخدمين
- ✅ إحصائيات شاملة

---

**رابط لوحة الإدارة:** `http://localhost:3000/admin`





